import 'package:flutter/material.dart';
import 'package:sumadd/Home_screen.dart';

class sumApp extends StatelessWidget {
  const sumApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomeScreen(),
    );
  }
}
